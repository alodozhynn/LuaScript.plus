codigo = codigo:gsub("consoleshow", "print")
codigo = codigo:gsub("warnconsole", "warn")
codigo = codigo:gsub("and", "then")
codigo = codigo:gsub("not", "else")
codigo = codigo:gsub("answer", "return")
codigo = codigo:gsub("stop", "end")
codigo = codigo:gsub("startrunning", "loadstring")
codigo = codigo:gsub("simu", "game")
codigo = codigo:gsub("searchfor", "GetService")
codigo = codigo:gsub("sourcelink", "HttpGet")
codigo = codigo:gsub("targetthing", "fromCharacter")
codigo = codigo:gsub("youthing", "toCharacter")
codigo = codigo:gsub("genvtake", "getgenv")
codigo = codigo:gsub("cbcopy", "toClipboard")
codigo = codigo:gsub("corecond", "SetCore")
codigo = codigo:gsub("answerwithdynamicmessage", "SendNotification")
codigo = codigo:gsub("what", "local")
codigo = codigo:gsub("moveablehumanoidthing", "HumanoidRootPart")
codigo = codigo:gsub("statuslife", "Health")
